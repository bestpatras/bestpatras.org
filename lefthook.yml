pre-commit:
  parallel: true
  commands:
    lint-code:
      run: "npm run format"
    commit-msg:
      scripts:
        "commitlint.sh":
          runner: bash
pre-push:
  parallel: false
  commands:
    check:
      run: "npm run check"
